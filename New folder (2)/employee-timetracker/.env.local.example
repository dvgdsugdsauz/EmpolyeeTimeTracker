# Copy this file to .env.local and set your backend URL to enable real API mode
# VITE_API_URL=http://localhost:8080

# When VITE_API_URL is NOT set, the app runs in mock/demo mode (no backend needed)
